# rk-mainline-sb

```shell
make
```

```shell
make clean
```

###### Meeting 20.03.2026
Goal:

Boot mainline u-boot and kernel. No secure-boot, it will be done next week.

Questions:
1) Which version of u-boot ? 
2) Which device tree for u-boot ?
3) which atf file ?
4) which op-tee file ?
3) Which version of kernel ?
4) Can we have an up-to-date device tree .dts for the board ? The one we have is de-compiled
5) which .its ?
6) Which version of mkimage ?


